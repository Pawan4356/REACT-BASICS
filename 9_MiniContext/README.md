Requirement for context API and React and importance of state management
Addition of inbuilt state management bye React - useContext()

Step 1

const UserContext = React.createContext()
export default UserContext;

after creating a context you have to create a provider